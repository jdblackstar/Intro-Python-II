# WIP

class Item:
    def __init__(self, name, description):
        self.name = name
        self.description = description
        #TODO: add types (1h, 2h, non-combat)
        #TODO: add stats (damage, durability)
        #TODO: add attributes (needed for x event, is torch)